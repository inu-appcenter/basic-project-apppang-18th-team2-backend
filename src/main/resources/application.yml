spring:
  application:
    name: apppang

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
    include: secret

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha